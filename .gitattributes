# В репозитории живут LF. Иначе генерируемые файлы (docs/INDEX.md, embeddings/manifest.json)
# отличаются между Windows-разработкой и Linux-CI, и проверка идемпотентности даёт ложный diff.
* text=auto eol=lf

*.png binary
*.pdf binary
*.xlsx binary
